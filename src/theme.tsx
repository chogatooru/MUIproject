import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#1a1a1a'
        },
        secondary: {
            main: '#1a1a1a'
        }
    },
    typography: {
        fontFamily: 'Roboto'
    }
})